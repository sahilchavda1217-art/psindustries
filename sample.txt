<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - P S Industries</title>
    <link href="index.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* index.css - Admin Login Page - Updated Footer and Header */
/* ================= RESET & BASE STYLES ================= */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html, body {
    width: 100%;
    overflow-x: hidden;
}

html {
    scroll-behavior: smooth;
    scrollbar-width: none;
    -ms-overflow-style: none;
}

html::-webkit-scrollbar {
    display: none;
}

body {
    font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    color: #333;
    line-height: 1.6;
    overflow-x: hidden;
    padding-top: 90px;
    background: linear-gradient(rgba(255, 255, 255, 0.95), rgba(255, 255, 255, 0.95)),
                url('factory-bg.jpg') center/cover fixed no-repeat;
    min-height: 100vh;
    position: relative;
    width: 100%;
}

body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url('TFO.jpg');
    background-size: cover;
    opacity: 0.08;
    z-index: -1;
    pointer-events: none;
}

body::-webkit-scrollbar {
    display: none;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
    width: 100%;
}

/* ================= ELEGANT FIXED HEADER ================= */
header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-bottom: 2px solid #DDBA7D;
    box-shadow: 0 4px 20px rgba(207, 75, 0, 0.15);
    padding: 15px 0;
    transition: all 0.3s ease;
}

header.scrolled {
    padding: 10px 0;
    background: rgba(255, 255, 255, 0.98);
    border-bottom-color: #CF4B00;
    box-shadow: 0 6px 25px rgba(207, 75, 0, 0.2);
}

.header-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 30px;
    margin-bottom: 10px;
    width: 100%;
    position: relative;
}

/* Logo and Company Name Container - Updated for Right Alignment */
.logo-company-container {
    display: flex;
    align-items: center;
    justify-content: flex-end; /* Changed from space-between to flex-end */
    gap: 25px;
    padding: 10px 20px;
    background: rgba(255, 255, 255, 0.9);
    border-radius: 15px;
    border: 2px solid #DDBA7D;
    box-shadow: 0 4px 15px rgba(207, 75, 0, 0.1);
    position: relative;
    overflow: hidden;
    width: 100%;
    margin-left: auto; /* Push to right */
    max-width: 600px; /* Limit width */
}

.logo-company-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(156, 198, 219, 0.1), transparent);
    z-index: 0;
}

/* Company info moved to the right */
.company-info {
    display: flex;
    flex-direction: column;
    position: relative;
    z-index: 1;
    text-align: right; /* Align text to right */
    margin-left: auto; /* Push to far right */
}

.company-info h1 {
    font-size: 2.2rem;
    font-weight: 800;
    color: #CF4B00;
    margin-bottom: 5px;
    letter-spacing: -0.5px;
    text-shadow: 1px 1px 3px rgba(207, 75, 0, 0.2);
    position: relative;
}

.company-info h1::after {
    content: '';
    position: absolute;
    bottom: -5px;
    right: 0; /* Changed from left: 0 to right: 0 */
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, transparent, #CF4B00); /* Reversed gradient */
}

.tagline {
    color: #666;
    font-size: 0.9rem;
    font-weight: 600;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    padding-right: 5px; /* Changed from padding-left */
    border-right: 3px solid #9CC6DB; /* Changed from border-left */
    text-align: right;
    text-align: right;
}

.logo-frame {
    background: white;
    border-radius: 12px;
    padding: 8px;
    box-shadow: 0 4px 15px rgba(207, 75, 0, 0.15);
    border: 2px solid #CF4B00;
    flex-shrink: 0;
    position: relative;
    z-index: 1;
    transition: all 0.3s ease;
    order: 2; /* Move logo to the right side */
    margin-left: 20px; /* Add space between text and logo */
    justify-content: left;
}

.logo-frame:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(207, 75, 0, 0.25);
}

.logo-frame img {
    border-radius: 8px;
    display: block;
    width: 60px;
    height: 60px;
    object-fit: cover;
}

/* ================= NAVIGATION ================= */
nav {
    padding: 15px 30px 0;
    width: 100%;
}

.nav-links {
    display: flex;
    justify-content: center;
    list-style: none;
    gap: 8px;
    background: linear-gradient(135deg, #9CC6DB, #DDBA7D);
    padding: 10px;
    border-radius: 15px;
    box-shadow: 0 4px 15px rgba(207, 75, 0, 0.2);
    position: relative;
    overflow: hidden;
    width: 100%;
}

.nav-links::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), transparent);
}

.nav-links li {
    position: relative;
}

.nav-links a {
    text-decoration: none;
    color: white;
    font-weight: 600;
    padding: 12px 25px;
    border-radius: 10px;
    transition: all 0.3s ease;
    font-size: 0.95rem;
    display: block;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(5px);
    position: relative;
    z-index: 1;
}

.nav-links a:hover {
    background: #CF4B00;
    color: white;
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(207, 75, 0, 0.3);
}

.nav-links a.active {
    background: #CF4B00;
    color: white;
    box-shadow: 0 3px 10px rgba(207, 75, 0, 0.3);
}

/* ================= LOGIN CONTAINER ================= */
.login-container {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: calc(100vh - 400px);
    padding: 40px 20px;
    margin: 50px 0;
    animation: fadeInUp 0.6s ease;
    width: 100%;
}

.login-form {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    padding: 50px 40px;
    border-radius: 20px;
    width: 100%;
    max-width: 500px;
    box-shadow: 0 20px 50px rgba(207, 75, 0, 0.2);
    border: 2px solid #DDBA7D;
    position: relative;
    overflow: hidden;
    margin-top: 100px;
}

.login-form::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 5px;
    background: linear-gradient(90deg, #CF4B00, #DDBA7D);
    z-index: 1;
}

.login-form::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(156, 198, 219, 0.05), rgba(221, 186, 125, 0.05));
    z-index: 0;
}

.login-form h2 {
    font-size: 2.2rem;
    color: #CF4B00;
    margin-bottom: 35px;
    text-align: center;
    font-weight: 700;
    position: relative;
    padding-bottom: 15px;
    z-index: 1;
}

.login-form h2::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 4px;
    background: linear-gradient(90deg, #CF4B00, #DDBA7D);
    border-radius: 2px;
}

/* ================= FORM STYLES ================= */
.form-group {
    margin-bottom: 25px;
    position: relative;
    z-index: 1;
}

.form-group label {
    display: block;
    font-weight: 600;
    color: #444;
    margin-bottom: 10px;
    font-size: 1.1rem;
    padding-left: 5px;
    border-left: 3px solid #9CC6DB;
}

.form-group input {
    width: 100%;
    padding: 16px 20px;
    border: 2px solid #DDBA7D;
    border-radius: 12px;
    font-size: 1rem;
    transition: all 0.3s ease;
    background: rgba(255, 255, 255, 0.9);
    color: #333;
    box-shadow: 0 4px 15px rgba(207, 75, 0, 0.1);
}

.form-group input:focus {
    outline: none;
    border-color: #CF4B00;
    box-shadow: 0 4px 20px rgba(207, 75, 0, 0.25);
    transform: translateY(-2px);
}

.form-group input::placeholder {
    color: #999;
}

/* ================= LOGIN BUTTON ================= */
.login-btn {
    width: 100%;
    padding: 18px 30px;
    background: linear-gradient(135deg, #CF4B00, #DDBA7D);
    color: white;
    border: none;
    border-radius: 12px;
    font-size: 1.1rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 10px;
    box-shadow: 0 6px 20px rgba(207, 75, 0, 0.3);
    position: relative;
    overflow: hidden;
    z-index: 1;
}

.login-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: all 0.6s ease;
    z-index: -1;
}

.login-btn:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 30px rgba(207, 75, 0, 0.4);
    background: linear-gradient(135deg, #DDBA7D, #CF4B00);
}

.login-btn:hover::before {
    left: 100%;
}

.login-btn:active {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(207, 75, 0, 0.3);
}

/* ================= MESSAGE STYLES ================= */
.success, .error {
    padding: 15px 20px;
    margin: 25px 0;
    border-radius: 12px;
    font-weight: 600;
    text-align: center;
    animation: fadeIn 0.5s ease;
    position: relative;
    z-index: 1;
    border: 2px solid transparent;
}

.success {
    background: linear-gradient(135deg, rgba(76, 175, 80, 0.1), rgba(76, 175, 80, 0.05));
    color: #2e7d32;
    border-color: #4CAF50;
    box-shadow: 0 4px 15px rgba(76, 175, 80, 0.2);
}

.error {
    background: linear-gradient(135deg, rgba(244, 67, 54, 0.1), rgba(244, 67, 54, 0.05));
    color: #c62828;
    border-color: #F44336;
    box-shadow: 0 4px 15px rgba(244, 67, 54, 0.2);
}

/* ================= 100% WIDTH FOOTER ================= */
footer {
    background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
    color: white;
    padding: 30px 0 15px;
    margin-top: 50px;
    border-top: 3px solid #CF4B00;
    position: relative;
    overflow: hidden;
    width: 100vw;
    left: 50%;
    transform: translateX(-50%);
}

footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: url('factory-bg.jpg') center/cover;
    opacity: 0.05;
    z-index: 0;
}

footer::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #CF4B00, #DDBA7D, #9CC6DB);
    z-index: 1;
}

/* Footer inner container */
.footer-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 25px;
    position: relative;
    z-index: 1;
}

.footer-links {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 25px;
    margin-bottom: 25px;
    position: relative;
    z-index: 1;
}

.footer-column h4 {
    font-size: 1.1rem;
    margin-bottom: 18px;
    color: #DDBA7D;
    font-weight: 600;
    position: relative;
    padding-bottom: 8px;
}

.footer-column h4::after {
    content: '';
    display: block;
    width: 35px;
    height: 2px;
    background: #CF4B00;
    margin-top: 6px;
    border-radius: 1px;
}

.footer-column a, .footer-column p {
    color: #aaa;
    text-decoration: none;
    margin-bottom: 10px;
    display: block;
    font-size: 0.9rem;
    transition: all 0.2s ease;
    padding: 5px 10px;
    border-radius: 4px;
}

.footer-column a:hover {
    color: white;
    background: rgba(207, 75, 0, 0.2);
    transform: translateX(5px);
    border-left: 2px solid #9CC6DB;
}

.support-link {
    display: flex;
    align-items: center;
    margin-bottom: 10px;
    padding: 8px 12px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 6px;
    font-size: 0.9rem;
}

.support-link svg {
    margin-right: 8px;
    color: #DDBA7D;
}

.footer-bottom {
    text-align: center;
    padding-top: 20px;
    border-top: 1px solid rgba(221, 186, 125, 0.2);
    position: relative;
    font-size: 0.85rem;
    color: #999;
    z-index: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding-left: 25px;
    padding-right: 25px;
}

/* Instagram corner positioned to left */
.instagram-corner {
    position: absolute;
    left: 25px;
    bottom: 15px;
    z-index: 1;
}

.instagram-icon-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 15px;
    background: linear-gradient(135deg, #CF4B00, #DDBA7D);
    color: white;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.85rem;
    transition: all 0.2s ease;
    border: 1px solid transparent;
}

.instagram-icon-btn:hover {
    background: linear-gradient(135deg, #DDBA7D, #CF4B00);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(207, 75, 0, 0.25);
    border-color: #9CC6DB;
}

.instagram-icon-simple {
    width: 20px;
    height: 20px;
}

/* ================= ANIMATIONS ================= */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes fadeIn {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
}

@keyframes float {
    0%, 100% {
        transform: translateY(0);
    }
    50% {
        transform: translateY(-10px);
    }
}

@keyframes subtleBg {
    0%, 100% {
        background-position: center center;
    }
    50% {
        background-position: calc(50% + 5px) calc(50% + 5px);
    }
}

body {
    animation: subtleBg 30s ease-in-out infinite;
}

.logo-frame:hover img {
    animation: float 2s ease-in-out infinite;
}

/* ================= RESPONSIVE DESIGN ================= */
@media (max-width: 1200px) {
    .container, .footer-inner {
        padding: 0 20px;
    }
}

@media (max-width: 992px) {
    .login-container {
        min-height: calc(100vh - 350px);
    }
    
    .logo-company-container {
        padding: 10px 15px;
        gap: 20px;
        max-width: 500px;
    }
    
    .company-info h1 {
        font-size: 2rem;
    }
}

@media (max-width: 768px) {
    body {
        padding-top: 130px;
        background-attachment: scroll;
    }
    
    .header-top {
        flex-direction: column;
        gap: 20px;
        padding: 0 20px;
    }
    
    .logo-company-container {
        flex-direction: column;
        text-align: center;
        gap: 15px;
        width: 100%;
        max-width: 100%;
        justify-content: center;
    }
    
    .company-info {
        text-align: center;
        margin-left: 0;
        order: 1;
    }
    
    .company-info h1 {
        font-size: 1.8rem;
    }
    
    .company-info h1::after {
        right: auto;
        left: 50%;
        transform: translateX(-50%);
        width: 80%;
    }
    
    .tagline {
        text-align: center;
        padding-right: 0;
        padding-left: 5px;
        border-right: none;
        border-left: 3px solid #9CC6DB;
        margin: 0 auto;
    }
    
    .logo-frame {
        order: 2;
        margin-left: 0;
        margin-top: 10px;
    }
    
    nav {
        padding: 15px 20px 0;
    }
    
    .nav-links {
        flex-wrap: wrap;
        gap: 10px;
        padding: 12px;
    }
    
    .nav-links a {
        padding: 10px 20px;
        font-size: 0.9rem;
    }
    
    .login-form {
        padding: 40px 30px;
        margin: 20px;
    }
    
    .login-form h2 {
        font-size: 1.8rem;
    }
    
    .instagram-corner {
        position: static;
        margin-top: 25px;
        text-align: center;
        left: 0;
    }
    
    .footer-bottom {
        text-align: center;
        padding: 15px 20px;
    }
}

@media (max-width: 576px) {
    body {
        padding-top: 150px;
    }
    
    .nav-links {
        flex-direction: column;
        align-items: stretch;
    }
    
    .nav-links a {
        text-align: center;
        padding: 14px;
    }
    
    .login-form {
        padding: 30px 20px;
    }
    
    .login-form h2 {
        font-size: 1.6rem;
    }
    
    .form-group input {
        padding: 14px 16px;
    }
    
    .login-btn {
        padding: 16px 20px;
    }
    
    .footer-links {
        grid-template-columns: 1fr;
        gap: 30px;
    }
    
    footer {
        padding: 25px 0 15px;
    }
    
    .footer-inner {
        padding: 0 15px;
    }
}

/* ================= LOADING STATE ================= */
.login-btn:disabled {
    opacity: 0.7;
    cursor: not-allowed;
    transform: none;
}

.login-btn:disabled:hover {
    transform: none;
    box-shadow: 0 6px 20px rgba(207, 75, 0, 0.3);
}

/* ================= FOCUS VISIBLE FOR ACCESSIBILITY ================= */
.form-group input:focus-visible {
    outline: 2px solid #CF4B00;
    outline-offset: 2px;
}

.login-btn:focus-visible {
    outline: 2px solid #9CC6DB;
    outline-offset: 2px;
}

/* ================= DECORATIVE ELEMENTS ================= */
.login-form .decoration {
    position: absolute;
    z-index: 0;
    opacity: 0.1;
}

.login-form .decoration:nth-child(1) {
    top: -50px;
    right: -50px;
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, #CF4B00 0%, transparent 70%);
    border-radius: 50%;
}

.login-form .decoration:nth-child(2) {
    bottom: -50px;
    left: -50px;
    width: 150px;
    height: 150px;
    background: radial-gradient(circle, #9CC6DB 0%, transparent 70%);
    border-radius: 50%;
}
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="header-top">
                <!-- Title and tagline moved to right -->
                <div class="logo-company-container">
                    <div class="company-info">
                        <h1>P S Industries</h1>
                        <p class="tagline">Secure Admin Access</p>
                    </div>
                    <div class="logo-frame">
                        <img src="final.png" alt="Company Logo" height="185px" width="185px">
                    </div>
                </div>
            </div>
            
            <nav>
                <ul class="nav-links">
                    <li><a href="home.php"><i class="fas fa-home"></i> Home</a></li>
                    <li><a href="#"><i class="fas fa-info-circle"></i> Overview</a></li>
                    <li><a href="#"><i class="fas fa-cogs"></i> Product Range</a></li>
                    <li><a href="qualityControl.php"><i class="fas fa-check-circle"></i> Quality Control</a></li>
                    <li><a href="#"><i class="fas fa-map-marker-alt"></i> Location</a></li>
                </ul>
            </nav>
        </header>
        
        <div class="login-container">
            <div class="login-form">
                <div class="decoration"></div>
                <div class="decoration"></div>
                <h2><i class="fas fa-lock"></i> Admin Login</h2>
                <form action="" method="post" id="loginForm">
                    <div class="form-group">
                        <label for="adminID"><i class="fas fa-user"></i> Admin ID</label>
                        <input type="text" name="adminID" id="adminID" placeholder="Enter your admin ID" required>
                    </div>
                    <div class="form-group">
                        <label for="password"><i class="fas fa-key"></i> Password</label>
                        <input type="password" name="password" id="password" placeholder="Enter your password" required>
                    </div>
                    <button type="submit" name="login" class="login-btn" id="loginBtn">
                        <span id="btnText">Log In</span>
                        <i class="fas fa-sign-in-alt" id="btnIcon"></i>
                    </button>
                </form>

                <?php
                include('db.php');
                if(isset($_POST["login"]))
                {
                    $id = $_POST["adminID"];
                    $pass = $_POST["password"];

                    if($id === "admin1234" && $pass === "psadmin1234")
                    {
                        echo "<div class='success'><i class='fas fa-check-circle'></i> Login successful! Redirecting to dashboard...</div>";
                        header("Refresh: 1.5; url=dashboard.php");
                        exit();
                    }
                    else {
                        echo "<div class='error'><i class='fas fa-exclamation-triangle'></i> Invalid credentials. Please try again.</div>";
                    }
                }
                ?>
            </div>
        </div>
    </div>
    
    <!-- 100% Width Footer -->
    <footer>
        <div class="footer-inner">
            <div class="footer-links">
                <div class="footer-column">
                    <h4>Quick Links</h4>
                    <a href="home.php"><i class="fas fa-arrow-right"></i> Home</a>
                    <a href="history.php"><i class="fas fa-arrow-right"></i> Company History</a>
                    <a href="#"><i class="fas fa-arrow-right"></i> Product Range</a>
                    <a href="qualityControl.php"><i class="fas fa-arrow-right"></i> Quality Control</a>
                </div>
                
                <div class="footer-column">
                    <h4>Support</h4>
                    <a href="clientSupport.php" class="support-link">
                        <i class="fas fa-headset"></i> Client Support
                    </a>
                    <a href="admin.php" class="support-link">
                        <i class="fas fa-user-shield"></i> Admin Support
                    </a>
                    <a href="technical-support.php" class="support-link">
                        <i class="fas fa-tools"></i> Technical Support
                    </a>
                </div>
                
                <div class="footer-column">
                    <h4>Contact Info</h4>
                    <p><i class="fas fa-envelope"></i> info@psindustries.com</p>
                    <p><i class="fas fa-phone"></i> +1 (555) 123-4567</p>
                    <p><i class="fas fa-map-marker-alt"></i> 123 Industrial Park, Manufacturing City</p>
                </div>
            </div>
            
            <div class="footer-bottom">
                <p>&copy; 2023 P S Industries. All Rights Reserved.</p>
                
                <!-- Instagram Icon in Left Corner -->
                <div class="instagram-corner">
                    <a href="https://www.instagram.com/psindustries2026/" target="_blank" class="instagram-icon-btn">
                        <i class="fab fa-instagram"></i> Follow our journey
                    </a>
                </div>
            </div>
        </div>

        
    </footer>

    <script>
        window.addEventListener('scroll', function() {
            const header = document.querySelector('header');
            if (window.scrollY > 20) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });

        document.getElementById('loginForm').addEventListener('submit', function(e) {
            const adminID = document.getElementById('adminID').value.trim();
            const password = document.getElementById('password').value.trim();
            
            if (!adminID || !password) {
                e.preventDefault();
                alert('Please fill in all fields');
                return false;
            }
            
            const btn = document.getElementById('loginBtn');
            const btnText = document.getElementById('btnText');
            const btnIcon = document.getElementById('btnIcon');
            
            btn.disabled = true;
            btnText.textContent = 'Authenticating...';
            btnIcon.className = 'fas fa-spinner fa-spin';
        });

        document.querySelectorAll('.form-group input').forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.classList.add('focused');
            });
            
            input.addEventListener('blur', function() {
                this.parentElement.classList.remove('focused');
            });
        });
    </script>
</body>
</html>